[
  {
    "$unwind": "$InvoiceLines"
  },
  {
    "$lookup": {
      "from": "Track",
      "let": { "trackId": "$InvoiceLines.TrackId" },
      "pipeline": [
        {
          "$match": {
            "$expr": {
              "$eq": [ "$Track.TrackId", "$$trackId" ]
            }
          }
        }
      ],
      "as": "TrackVec"
    }
  },
  { "$unwind": "$TrackVec" },
  {
    "$project": {
      "_id": 0,
      "TrackName": "$TrackVec.Track.Name",
      "Date": "$InvoiceDate",
      "TotalRevenue": {
        "$multiply": [ "$InvoiceLines.UnitPrice", "$InvoiceLines.Quantity" ]
      }
    }
  },
  {
    "$group": {
      "_id": {
        "TrackName": "$TrackName",
        "Date": "$Date"
      },
      "TotalRevenue": { "$sum": "$TotalRevenue" }
    }
  },
  {
    "$project": {
      "_id": 0,
      "TrackName": "$_id.TrackName",
      "Date": "$_id.Date",
      "TotalRevenue": "$TotalRevenue"
    }
  },
  { "$sort": { "_id.Date": 1, "TotalRevenue": -1 } }
]